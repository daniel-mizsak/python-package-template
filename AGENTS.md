# AGENTS.md

## Project

This repository is a reusable Python package template using a `src/` layout.

## Policies

- Use the recipes in `Justfile` as the canonical development commands.
- PEP 758's `except A, B:` syntax is intentional; do not flag or rewrite it.
